# AIWave 領域詞彙

> 2026 雲湧智生黑客松統一資訊命題參賽作品。此文件定義跨產品、前端、後端、Agent、MCP 與簡報共同使用的語言。

## 產品

**AIWave**：會員目標驅動的 AI 生活任務平台。跨點數、廠商與群組規劃、比較、確認、執行及追蹤生活服務。  
_Avoid_：第二個 OPENPOINT、社區管理 App、服務型錄、ERP。

**AI 生活管家**：AIWave 的自然語言操作層。能讀取 capability registry、規劃多步驟任務及調用工具；不直接決定金額、權限或狀態。  
_Avoid_：FAQ、萬能聊天機器人、完全自主代理。

**生活目標（life goal）**：會員用自己的語言描述想完成的結果，例如「爸媽週六要來，幫我把家裡整理好」。

**生活任務（life task）**：由生活目標拆出的可執行工作單元，含相依順序、輸入、工具、確認層級及狀態。它是 AIWave 的招牌 domain object。  
_Avoid_：待辦清單（未必可執行）、聊天回覆。

**任務計畫（task plan）**：Planner 對單一生活目標產生的結構化步驟集合。通過工具白名單、schema、權限與規則檢查後，才能進入預覽或執行。

**Capability Registry**：AI 可使用功能的單一清單，記錄工具名稱、參數、權限、風險層級與可用性；HTTP 與 MCP 應共用同一 domain capability。

## 角色、身分與範圍

**會員（member）**：產品的主要使用者，對應官方消費者端。會員不一定住在已接入的社區。  
_Avoid_：一律稱住戶、同時扮演廠商或平台營運者。

**合作廠商人員**：代表服務廠商接收需求、報價、聯繫會員與更新履約狀態的人員。

**平台營運者**：管理服務契約、接入狀態、稽核及未滿足需求情報的人員。

**範圍（scope）**：資料及操作的歸屬。核心值為 `individual` 與 `group`。

**Active scope**：會員當下代表自己或某個群組操作的明確上下文。所有群組讀寫都必須帶 scope，不能以「登入後曾選過」的隱含狀態猜測。

**群組（Group）**：多人共享指定資料與任務的集合；`group_type` 可為 `family`、`couple`、`dorm`、`community`。家庭與社區不是兩套系統。

**群組成員（GroupMember）**：會員在某群組中的關係，含 role、權限、加入狀態與通知設定。同一會員可加入多個群組。

**群組槓桿**：經會員同意後，以共同需求、共同時段、代辦或安心通知提高生活任務效益。  
_Avoid_：預設公開個人需求、只等同團購折扣。

## AI 權限與互動

**直接執行**：低風險讀取與導覽，例如查點數、查狀態、開啟頁面；執行後須可見。

**預覽後確認**：設定、表單及資料共享，例如建立提醒、產生留資表單、切換共享範圍。

**交易確認**：下單、兌換、接受報價、付款或取消等高影響操作；必須顯示標的、金額、資料用途及後果，確認後寫入稽核。

**執行摘要**：對使用者顯示正在查什麼、呼叫何種工具、使用哪些依據與結果。  
_Avoid_：原始 chain-of-thought、hidden prompt、憑證或不必要個資。

## 服務、表單與履約

**官方服務來源**：官方資料中的服務分類或整合來源，不等於實際履約公司。  
_Avoid_：官方廠商、合作廠商。

**合作廠商（Vendor）**：實際提供服務、報價及履約的業者。可有多個服務方案及據點。

**廠商服務方案（Offering）**：廠商可供媒合的一項具體服務，記錄價格模型、時段、範圍及限制。

**服務媒合**：依服務類型、地區、時段、預算、緊急程度、服務範圍及評分等條件產生可重算候選與比較理由。

**彈性留資表單／諮詢單**：官方命題中的 lead-capture flow。表單定義由官方 `pms_form`、`pms_form_group`、`pms_form_topic` schema 驅動；會員完成填答後形成可交付廠商的諮詢單。

**題組引擎**：讀取表單 schema、套用已知會員資料、處理跳題與驗證，並可由聊天選項或完整表單呈現的共用能力。

**履約事件**：廠商接單、報價、排程、到場、完成與異常等狀態變化。AI、首頁、訂單頁及廠商後台必須讀取同一事件來源。

**訂單折疊群組**：依待確認、進行中、待取件／到場、已完成及異常分組的訂單呈現方式；摘要與完整時間軸分層顯示。

## 廠商接入

**統一服務契約**：平台定義的廠商、方案、可用性、諮詢、報價、訂單及事件 schema，供 Client、fake server、正式 API、測試與 MCP 共用。

**VendorClient**：平台 domain 使用的廠商能力介面。`MockVendorClient` 與 `RealVendorClient` 是可替換 HTTP adapter。

**Fake vendor server**：獨立執行、實作 OpenAPI 3.0 契約的上游模擬服務，支援 seed、reset、延遲及故障注入。  
_Avoid_：前端 fixture、平台後端、正式品牌 API。

**VENDOR_MODE**：平台後端的 composition 設定，用來選擇 fake 或 real adapter；不可由瀏覽器切換。

**標準接入**：廠商直接實作統一服務契約。

**轉接接入**：平台 adapter 將既有廠商 API 映射為統一服務契約。

**工作台接入**：沒有 API 的廠商使用廠商後台完成同一 domain workflow。

## 點數、推薦與資料

**點數帳本**：競賽期間可重算的 OPENPOINT 情境餘額、異動及到期資料；取得正式 API 前不得宣稱是即時正式帳戶。

**點數最佳化**：確定性計算點數、優惠券及支付條件，輸出折抵與應付金額；LLM 只解釋及納入任務計畫。

**行為指紋**：以會員 id 及 hash 解析跨服務資料是否屬於同一人。  
_Avoid_：行為序列。

**行為軌跡**：同一會員跨服務、跨時間排序後的行為序列。

**行為特徵檔**：由行為軌跡可重算得到的頻率、週期、常用服務、時段與偏好，不是假裝以少量 seed 訓練的黑箱模型。

**混合推薦**：規則及特徵產生候選、分數和硬性排除；AI 結合聊天語境排序並解釋。

**推薦回饋**：「不感興趣」或復原形成的軟性偏好，只影響該項及合理相似內容，不永久關閉整個推薦功能。

**硬性限制**：過敏、飲食禁忌及會員明確禁止內容；推薦時必須排除，和不感興趣分開。

**個人化同意**：會員允許使用訂單、表單及結構化對話意圖產生個人化的授權；可撤回並清除衍生特徵。

## 相鄰平台邊界

**OPENPOINT**：統一生態帳號、點數、優惠、支付及服務資產；AIWave 消費這些資產，不重做完整平台。

**CommunityConnector**：未來連接智生活類社區系統的介面，可讀取經授權的群組、設施、公告與待辦；AIWave 不重做門禁、包裹、管理費或物業系統。
